const express = require("express");
const _ = express.Router();

// _.use("/demo", require("./api/demo.api"));

// //another example
// _.use("/category", require("./api/category.api"));
// //.... any other routes needed

module.exports = _;
