//require express essentials
const express = require("express");
const _ = express.Router();

// //require the controlller function
// const { demoController } = require("../../controllers/demo.controller");
// const { demoMiddleware } = require("../../middleware/demo.middleware");

// //defining the routes and the methods with controllers and middlewares
// _.route("/demo-api").post(demoController.demoFunction);
// _.route("/get-info").get(demoMiddleware, demoController.anotherDemoFunction);

// module.exports = _;
